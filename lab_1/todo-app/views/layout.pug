doctype html
html(lang="ru")
  head
    meta(charset="utf-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title #{appName}
    link(rel="stylesheet" href="/styles.css")
  body
    header.container
      h1 #{appName}
      nav
        a(href="/") Список
        a(href="/new") Новая задача
        a(href="/about") О нас
    main.container
      block content
    footer.container
      small © #{new Date().getFullYear()} · Учебный проект
